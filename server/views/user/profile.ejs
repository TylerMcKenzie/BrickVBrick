<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/page/head') %>
  </head>
  <body>
    <%- include('../partials/page/nav') %>

    <div class="profile-container">
      <div class="profile-name">
        <h3>Username: <%= user.username %></h3>
      </div>

      <div class="stats-container">
        <!-- <div class="title">
          <h4>Games Won</h4>
        </div>

        <div class="games-won">
          <p>
            <%= user.gamesWon %>
          </p>
        </div>

        <div class="title">
          <h4>Games Lost</h4>
        </div>

        <div class="games-lost">
          <p>
            <%= user.gamesLost %>
          </p>
        </div>
        -->

        <div class="title">
          <h4>Total Games Played</h4>
        </div>

        <div class="games-played">
          <p>
            <%= userScores.length %>
          </p>
        </div>

        <!-- <% if(user.winLoseRatio()) { %>
          <div class="ratio">
            <p>
              <%= user.winLoseRatio() %>
            </p>
          </div>
        <% }; %> -->
      </div>
    </div>

    <div class="profile-scores">
      <ul>
        <% for(var i=0; i<userScores.length; i++) { %>
          <li>
            <p>
              Score:
              <%= userScores[i].score %>
            </p>
            <p>
              Date Played:
              <%= userScores[i].date.toLocaleDateString() %>
            </p>
          </li>
          <% } %>
      </ul>
    </div>

    <%- include('../partials/page/scripts') %>
  </body>
</html>
